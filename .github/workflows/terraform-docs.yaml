name: Update Terraform documentation

on:
  pull_request:
  push:
    branches:
      - 2/edge

jobs:
  terraform-docs:
    name: Update Terraform documentation
    uses: canonical/operator-workflows/.github/workflows/generate_terraform_docs.yaml@main
    secrets: inherit
    with:
      terraform-directory: terraform,terraform/charm,terraform/product
